"use server";

export async function printReceipt(challanNumber: string) {
  // Placeholder for a real printing integration (e.g., network printer, fiscal printer, etc).
  // For now we just no-op so the UI can verify the flow.
  console.log("printReceipt placeholder:", challanNumber);
}

